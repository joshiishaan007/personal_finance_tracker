#!/bin/sh
# SessionStart: print the active coding-standards reference banner.
echo "Personal Finance Tracker standards active — CLAUDE.md loaded. Single Next.js 14 app. Reference feature: transaction."
echo "Backend: 4-layer (route handler→controller→service→repository→model), no try/catch, userId-scope every query, catchRoute wrap, runtime='nodejs'."
echo "Frontend: data only in hooks/use<Feature> → api + ENDPOINTS; element/component/widget tiers; Tailwind tokens only."
echo "Gates: pnpm typecheck && pnpm lint && pnpm build (+ pnpm test). Skills: coding-standards, design-system, client, server, shared, database."
exit 0
